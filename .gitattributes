# Normalize all text files to LF in the repo and working tree.
# Keeps line endings consistent across OSes and matches Biome's default.
* text=auto eol=lf

# Binary files — never touch.
*.png binary
*.ico binary
